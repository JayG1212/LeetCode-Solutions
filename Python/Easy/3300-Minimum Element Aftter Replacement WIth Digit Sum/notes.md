# Pattern: Brute Force
# Time complexity: O(N * D) = O(N)
# Reflection:
    I had at first thought this was O(N^2) because I used a nested loop. However since the inner loop is of a different size (of size D), it would be O(N * D); which simplifies to O(N).
    I should probably try to learn how to find digits using mathematical operators in future problems like this one.