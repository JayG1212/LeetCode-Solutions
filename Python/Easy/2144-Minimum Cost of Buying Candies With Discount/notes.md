# Pattern: Sorting
# Time complexity: O(N)
# Reflection:
    This one I made use of the built-in sort function, which I am not used to using since we don't allow our student's in CIS 122 to use them. But I could easily just add a sort algorithm tto do it if I had to.