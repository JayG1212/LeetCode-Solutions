# Pattern: Two Pointers
# Time complexity: O(n)
# Reflection:
    Pretty much the same as problem 26, but easier.
    The only thing learned from this and the previous one is that we can use two pointers to remove duplicates, without needing a nested loop.