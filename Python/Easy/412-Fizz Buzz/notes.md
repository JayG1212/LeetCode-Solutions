# Pattern: Iteration
# Time complexity: O(N)
# Reflection:
    Fizz Buzz is a classic, not much new learned since I have done this problem a lot
    I did learn that using "for i in range(n)" was a bad idea with an integer input "n"
    This is because it starts at "i = 0", and ends at i = "n - 1"
    Used a while loop instead.